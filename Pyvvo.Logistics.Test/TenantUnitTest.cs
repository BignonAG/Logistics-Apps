using NUnit.Framework;
using Pyvvo.Logistics.Core;
using Pyvvo.Logistics.Model;
using System;
using System.Collections.Generic;
using System.Text;

namespace Pyvvo.Logistics.Test
{
    public class TenantUnitTest
    {
        [Test]
        public void CreateTestTenantForUser()
        {
            // var tenant = new Tenant();
            // tenant.CreatedOn = DateTime.Now;
            // tenant.UpdatedOn = DateTime.Now;
            // tenant.IsActive = true;
            // tenant.Status = new StatusCore().GetStatus(1).Result;
            // TenantCore tenantCore = new TenantCore();
            // var isCreated =  tenantCore.CreateTenant(tenant).Result;
            // Assert.IsTrue(isCreated);
        }
    }
}
